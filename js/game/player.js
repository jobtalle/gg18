function Player(controller) {
    this.listen(controller);
}

Player.prototype = {
    update(timeStep) {
        
    },
    
    render(context) {
        
    },
    
    listen(controller) {
        
    }
}